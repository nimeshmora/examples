name: rabbitmq-demo
spec:
  cluster: "@{cluster}"
  description: "Route group for the RabbitMQ message-isolation demo"
  match:
    any:
      - label:
          key: demo
          value: rabbitmq
  endpoints:
    - name: publisher
      target: http://publisher.rabbitmq-demo.svc:8080
